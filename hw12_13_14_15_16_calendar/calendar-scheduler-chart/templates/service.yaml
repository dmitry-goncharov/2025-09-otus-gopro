apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-scheduler-service
  labels:
    app: {{ .Values.scheduler.appLabel }}
spec:
  clusterIP: None
  selector:
    app: {{ .Values.scheduler.appLabel }}
