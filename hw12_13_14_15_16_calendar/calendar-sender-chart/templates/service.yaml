apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-sender-service
  labels:
    app: {{ .Values.sender.appLabel }}
spec:
  clusterIP: None
  selector:
    app: {{ .Values.sender.appLabel }}
